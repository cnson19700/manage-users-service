export enum APP_CONSTANTS {
    APP_INFO = 'app-info',
    APP_ERROR = 'app-error',
    APP_WARN = 'app-warn',
    HTTP = 'http-app',
    MAIL = 'http-mail',
    DEVELOPMENT = 'development',
    PRODUCTION = 'production',
    TEST = 'test',
    DEBUG = 'debug',
    CONFIG_FILE_NOT_FOUND = 'Configuration file not found when starting the server',
}
